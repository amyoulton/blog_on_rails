<nav class="navbar navbar navbar-expand-lg bg-light justify-content-between">
    <a class="navbar-brand" href="/">Blog On Rails</a>
        <div>
          <ul class="navbar-nav mr-auto'">
            <li class="nav-item">
              <%= link_to 'Home', '/' %> 
            </li>
            <li class="nav-item">
              <%= link_to 'New Post', '/posts/new' %> 
            </li>
            <li class="nav-item">
              <%= link_to 'New User', '/users/new' %> 
            </li>
    <% if user_signed_in? %>
     <li class="nav-item user-link"><%= link_to current_user.name, edit_user_path(current_user.id) %> </li>
      <li><%= link_to "Logout", session_path, method: :delete, class: 'btn btn-danger log-btn' %> </li>
    <% else %>
      <li><%= link_to "Log In", new_session_path, class: 'btn btn-success log-btn'%></li>
    <% end %>
          </ul>
          </div>
      </nav>